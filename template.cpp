//Run with $g++ -o a <filename>.cpp ; ./a.exe
//$measure-command { ./a.exe }
#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
  cin.tie(nullptr);
  ios::sync_with_stdio(false);
  
  return 0;
}